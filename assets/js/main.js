(()=>{'use strict';document.querySelectorAll('a[href^="#"]').forEach(a=>a.addEventListener('click',e=>{const id=a.getAttribute('href');const t=document.querySelector(id);if(!t)return;e.preventDefault();window.scrollTo({top:t.getBoundingClientRect().top+scrollY-70,behavior:'smooth'})}));const p=document.getElementById('linePopup');const c=p?.querySelector('button');let shown=false;const check=()=>{const max=document.documentElement.scrollHeight-innerHeight;if(max>0&&!shown&&scrollY/max>=.6){p?.classList.add('is-visible');p?.setAttribute('aria-hidden','false');shown=true}};addEventListener('scroll',check,{passive:true});addEventListener('load',check);c?.addEventListener('click',()=>{p.classList.remove('is-visible');p.setAttribute('aria-hidden','true')});})();
